"use client";

// A one-line wrapper that marks the third-party component as a Client
// Component, so Server Components can render it.
import { Rating } from "../_vendor/acme-rating";

export default Rating;
